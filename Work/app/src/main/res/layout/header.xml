<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable name="fragment" type="usausa.github.io.work.view.shared.HeaderFragment"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/metroTeal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/label_user_id"
            android:textColor="@color/white"
            android:visibility="@{fragment.userIdEnable}"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="@{fragment.userId}"
            android:textColor="@color/white"
            android:textAlignment="textEnd"
            android:visibility="@{fragment.userIdEnable}"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/label_terminal_no"
            android:textColor="@color/white"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="@{fragment.terminalNo}"
            android:textColor="@color/white"
            android:textAlignment="textEnd"/>

    </LinearLayout>

</layout>
